language: node_js
branches:
  only:
    - master
    - /^greenkeeper/.*$/
cache:
  yarn: true
  directories:
    - node_modules
notifications:
  email: false
node_js:
  - node
script:
  - yarn test
  - yarn build
#  - npm run test:prod && npm run build

#after_success:
#  - npm run report-coverage
#  - npm run deploy-docs
#  - npm run semantic-release


# Codeclimate config
# env:
#   global:
#    - CC_TEST_REPORTER_ID=37ef998734eb034cd7da94c2257f8f9bfb5770bc9470c3759cfe8ca72eda1900
# rvm:
#  - 2.2.0
# before_script:
#   - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
#   - chmod +x ./cc-test-reporter
#   - ./cc-test-reporter before-build
# script:
#   - bundle exec rspec
# after_script:
#   - ./cc-test-reporter after-build --exit-code $TRAVIS_TEST_RESULT
